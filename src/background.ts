chrome.runtime.onMessage.addListener(function (message, sender, sendResponse) {
  console.debug("Background received message:", message);
  // Do the translator scraping work here!
  chrome.storage.session.;
});

export {};
